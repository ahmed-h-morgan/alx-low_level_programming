#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: an integer
 * Return: 0 success
*/
int _abs(int i)
{
	/*int i;*/

	/*i = _abs(i);*/
	if (i < 0)
	{
		return (i * -1);
	}
	else
	{
		return (i);
	}
}
