#include "main.h"

/**
 * _isupper - returns 1 for the upper char
 * @c: the character
 * Return: largest number
 *
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
