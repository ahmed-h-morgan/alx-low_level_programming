#include "main.h"

/**
 * _isupper - returns 1 for the upper char
 * @c: the character
 * Return: largest number
 *
*/

int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
	{
		return (1);
	}
	return (0);
}
